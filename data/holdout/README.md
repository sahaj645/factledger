# Holdout corpus — closed

These documents are the generalization test. They are not opened, read, sampled,
or referenced by any code or working session until the cold-run stage. Nothing is
tuned against them. See CLAUDE.md section 10.
